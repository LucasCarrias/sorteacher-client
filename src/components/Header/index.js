import React from 'react';
import { Link } from 'react-router-dom';
import './headerStyles.css';

const Header = () => {
  return(
    <div>
      <header id="main-header">Sorteacher</header>
    </div>
  );
};

export default Header;